<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.main.identification.repository.CommonRepository">
  <select id="selectSequenceId" resultType="int" parameterType="String">
    SELECT NEXTVAL(#{seqName});
  </select>   
</mapper>